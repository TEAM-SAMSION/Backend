spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME} # 환경 변수
    password: ${DB_PASSWORD} # 환경 변수
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update